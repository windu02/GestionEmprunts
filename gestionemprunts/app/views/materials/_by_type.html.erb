<% 
	if @ids != nil
		@ids.each do |id|
			materialtype = Materialtype.find_by_id(id)
			if materialtype != nil
				materials = materialtype.associated_materials
%>
			<article id="materials">
				<%= render materials %>
			</article>
			<% end %>	
		<% end %>	
	<% end %>
